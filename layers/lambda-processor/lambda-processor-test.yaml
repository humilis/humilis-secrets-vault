---
lambda-processor-test:
    description:
        A dummy environment needed to run integration tests on the 
        lambda-processor layer
    tags:
        "cost:chapter": "data"
        "cost:squad": "product"
        "cost:project": "nfh-event-tracker"
    layers:
        - {layer: storage}
        - {layer: firehose-rsc}
        - {layer: io-streams}
        - {layer: lambda-processor, io_layer_name: io-streams}
