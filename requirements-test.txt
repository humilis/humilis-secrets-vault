# test requirements
boto3
humilis >= 0.3.0
mock
pytest
pytest-cov
tox

# We need to install this layer to be able to test it
.

# Also need these supporting layers to run the integration test suite
git+https://github.com/InnovativeTravel/humilis-streams
git+https://github.com/InnovativeTravel/humilis-filter
