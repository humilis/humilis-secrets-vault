---
secrets-vault-test:
    description:
        A dummy environment used for testing the secrets-vault layer.
    tags:
        "cost:chapter": "data"
        "cost:squad": "product"
        "cost:project": "nfh-event-tracker"
    layers:
        # The storage layer
        - {layer: io-streams}
        - {layer: lambda-processor, io_layer_name: io-streams}
        - {layer: secrets-vault, associated_processors: ['lambda-processor']}
